# DESIGN.md

## 项目概述

**项目主题**：电商产品评论情感分析 - 数据挖掘与机器学习实践平台

这是一个教学导向的数据挖掘与机器学习实践平台，展示完整的ML工作流程，从数据采集到模型训练与评估。

## 气质与意象

**关键词**：科学严谨、教育友好、流程清晰、专业可信

**场景意象**：一个现代化的数据科学实验室，白板上绘制着算法流程图，屏幕上显示着动态的数据可视化图表，研究人员在终端前调试模型参数，整体氛围既专业又亲切。

## 视觉策略

- **图标与图形**：使用数据科学相关的图标（图表、神经网络节点、数据流）
- **图形语言**：几何化、结构化，强调数据的有序性和算法的逻辑性
- **摄影方向**：不使用人物照片，以抽象的数据可视化为主

## 配色方案

每个颜色附意象来源：

- **主色调（深蓝）** `#1e40af`: 源自数据科学工具的标准配色，象征专业与严谨
- **辅助色（橙色）** `#f97316`: 数据点的高亮颜色，象征洞察与发现
- **背景色（浅灰）** `#f8fafc`: 实验室白板的清爽感，提供中性工作背景
- **成功色（绿色）** `#10b981`: 正向情感标签，数据清洗成功的状态
- **警告色（琥珀）** `#f59e0b`: 异常数据提示，需要关注的步骤
- **错误色（红色）** `#ef4444`: 负向情感标签，处理失败的提示

## 字体排版

- **主字体**：思源黑体（Source Han Sans）- 清晰易读，适合技术内容
- **代码字体**：JetBrains Mono - 专为开发者设计的等宽字体
- **标题节奏**：使用 2xl/xl/lg 的层级，算法名称用 xl 加粗
- **正文节奏**：14px 基础，12px 用于参数说明，10px 用于图表注释

## 动效与交互

- **运动性格**：平滑、可控、响应式
- **缓动曲线**：ease-out 用于展开，ease-in 用于收起
- **悬停状态**：卡片微微上浮（translateY -2px），颜色加深
- **过渡时长**：200ms 快速响应，500ms 用于复杂动画
- **滚动交互**：数据表格支持虚拟滚动，长列表平滑加载

## 页面结构

采用**流程化布局**，按数据挖掘工作流组织：

1. **顶部导航**：项目标题 + 进度指示器（当前步骤）
2. **左侧边栏**：功能模块入口（数据采集、预处理、ML、DL、对比分析）
3. **中央工作区**：当前模块的主操作界面
4. **右侧面板**：实时参数面板 + 结果预览

### 各区域内容构成

- **数据采集区**：爬虫控制面板 + 数据源选择 + 实时采集日志
- **预处理区**：数据表格 + 统计面板 + 清洗操作按钮
- **ML区**：算法选择 + 参数调节滑块 + 结果可视化图表
- **DL区**：网络架构可视化 + 训练进度条 + 准确率曲线
- **对比区**：模型卡片 + 性能对比表格 + 结论解释

## 设计禁忌

- ❌ 不要使用卡通化或过度装饰的图标
- ❌ 不要使用过多的渐变和发光效果（保持专业学术风格）
- ❌ 不要在数据处理区使用纯黑背景（影响数据可读性）
- ❌ 不要隐藏关键参数和算法细节（教学导向需要透明度）
- ❌ 不要使用模糊的技术术语，必须提供清晰的中文解释